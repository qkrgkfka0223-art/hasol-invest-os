name: HASOL Quant Unit Tests

on:
  pull_request:
    branches: [main]
    paths:
      - 'hasol_quant/**'
      - 'scripts/run_full_market_quant.py'
      - 'scripts/__init__.py'
      - 'tests/test_quant_engine.py'
      - 'requirements-quant.txt'
      - '.github/workflows/full-market-quant.yml'
      - '.github/workflows/quant-unit-tests.yml'
  push:
    branches: [main]
    paths:
      - 'hasol_quant/**'
      - 'scripts/run_full_market_quant.py'
      - 'scripts/__init__.py'
      - 'tests/test_quant_engine.py'
      - 'requirements-quant.txt'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: requirements-quant.txt
      - run: |
          python -m pip install --upgrade pip
          pip install -r requirements-quant.txt
      - run: python -m pytest -q tests/test_quant_engine.py
      - run: python -m scripts.run_full_market_quant --help
